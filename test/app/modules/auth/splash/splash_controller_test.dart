import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:biodriver/app/modules/auth/splash/splash_controller.dart';
import 'package:biodriver/app/modules/auth/auth_module.dart';

void main() {
  initModule(AuthModule());
  // SplashController splash;
  //
  setUp(() {
    //     splash = AuthModule.to.get<SplashController>();
  });

  group('SplashController Test', () {
    //   test("First Test", () {
    //     expect(splash, isInstanceOf<SplashController>());
    //   });

    //   test("Set Value", () {
    //     expect(splash.value, equals(0));
    //     splash.increment();
    //     expect(splash.value, equals(1));
    //   });
  });
}
