import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:biodriver/app/modules/home/custom_map/custom_map_controller.dart';
import 'package:biodriver/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // CustomMapController custommap;
  //
  setUp(() {
    //     custommap = HomeModule.to.get<CustomMapController>();
  });

  group('CustomMapController Test', () {
    //   test("First Test", () {
    //     expect(custommap, isInstanceOf<CustomMapController>());
    //   });

    //   test("Set Value", () {
    //     expect(custommap.value, equals(0));
    //     custommap.increment();
    //     expect(custommap.value, equals(1));
    //   });
  });
}
